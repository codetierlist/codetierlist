'use client';

import { Display } from '@fluentui/react-components';

export default function NotFound () {
    return (
        <main>
            <Display>404</Display>
        </main>
    );
}
